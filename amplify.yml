version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - yarn set version stable
        - yarn install --immutable
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
