rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all collections
    // Your portfolio data is public, so anyone can read it
    match /{document=**} {
      allow read: if true;
      allow write: if false; // Only allow writes through admin SDK or console
    }
  }
}
